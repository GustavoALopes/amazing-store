

spring:
  jpa:
    hibernate:
      ddl-auto: none
  datasource:
    url: jdbc:postgresql://localhost:5432/amazing-store-test
    username: postgres
    password: secret
  flyway:
    url: jdbc:postgresql://localhost:5432/amazing-store-test
    user: postgres
    password: secret
    schema: public
    enabled: true